apiVersion: v1
kind: Service
metadata:
  name: springboot-service
spec:
  type: NodePort
  selector:
    app: springboot-app
  ports:
    - port: 80
      targetPort: 8081
      nodePort: 30080  # Accessible at http://<Any-Cluster-Node-IP>:30080